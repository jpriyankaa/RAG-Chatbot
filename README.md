# RAG-Chatbot
This project implements a fully local (RAG) chatbot using Streamlit for the UI, FAISS for vector searching, HuggingFace sentence-transformer embeddings, and Ollama as the local LLM backend. uploading PDF/Text docs, chunking, storing the embeddings in FAISS, answering user queries by retrieving relevant chunks,generating responses using an LLM.
